# Frontend

Streamlit MVP user interface placeholder.

Future files here should focus on presentation, user input, layout, and view-level state only. Business logic should live in `app/services` or lower-level modules.

